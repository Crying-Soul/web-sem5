extends index.pug

block content
  .card.shadow-sm.mt-4
    .card-header.bg-success.text-white
      h2.mb-0 Друзья пользователя
    .card-body.bg-white
      ul.list-group.list-group-flush
        each friend in friends
          +friendItem(friend)
